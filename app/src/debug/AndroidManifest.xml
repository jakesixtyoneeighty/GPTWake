<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only adb control surface. Never shipped in a release build. -->
        <receiver
            android:name=".ControlReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.desmond.gptwake.CTRL" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
